// Content script — assists with grabbing selected text for the context menu
// The actual selection text is handled by chrome.contextMenus info.selectionText
// This script is kept minimal; it can be extended if DOM-level extraction is needed.

(function () {
  // Nothing needed for now — context menu selection is handled natively
})();
